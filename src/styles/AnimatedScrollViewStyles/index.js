import { StyleSheet } from "react-native";
import { colors } from "../../assets/colors";

export const animatedScrollViewStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: colors.white,
        zIndex: 2
    }
})